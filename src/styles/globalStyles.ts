import {StyleSheet} from 'react-native';

const globalStyles = StyleSheet.create({
  container: {},
  textCenter: {
    textAlign: 'center',
  },
  marginTopHalf: {
    marginTop: 8,
  },
});

export default globalStyles;
